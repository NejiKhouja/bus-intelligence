.env
.claude
__pycache__/
*.pyc
.ipynb_checkpoints/
logs/
models/models_version.json

# Too large for GitHub (>100MB hard limit) -- see Dockerfile.render for how production
# gets its artifacts instead (winicari_reference_slim.db + models/ticket_anomaly/*.joblib
# top-level only, both git-tracked and well under the limit). Regenerate locally via
# reference_db.populate_tickets_station_daily / populate_tickets_station_trip_daily and
# models.ticket_anomaly.train_stations() -- see docs/WEBSERVICES_NEEDED.md.
data/reference/winicari_reference.db
models/ticket_anomaly/stations/

# Client hand-off embed (PHP/HTML/CSS/JS anomaly-detection widget for the company's own
# dashboard, see embed/README.md) -- config.php holds the real Render API key, never
# committed; config.example.php (tracked) is the template to fill in.
embed/config.phpdata/live_ingest/
