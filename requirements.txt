# WiniCari AI layer — PRODUCTION runtime dependencies (the FastAPI serving image only).
# Dashboard, notebooks, and the RAG chatbot are NOT part of this deployment — see
# requirements-dev.txt for those. Pinned to the versions installed in the `bus-intelligence`
# conda env used for training and serving.

# --extra-index-url pulls the CPU-only torch wheel instead of the default CUDA-bundled one --
# this service only ever does CPU inference, and the CUDA wheel is several GB larger for
# nothing. See docs/DEPLOYMENT.md.
--extra-index-url https://download.pytorch.org/whl/cpu

# API / serving
fastapi==0.138.0
uvicorn==0.49.0
pydantic==2.13.4

# Data / MongoDB
pymongo==4.17.0
pandas==2.3.3
numpy==2.4.6
python-dotenv==1.2.2
# pandas needs one of these to read/write .parquet -- not a direct pandas dependency, easy
# to miss (this was in fact missing from the original requirements.txt and only "worked"
# in dev because pyarrow was pulled in transitively by an unrelated package).
pyarrow==24.0.0
# src/data/webservices.py (live GPS/ticket webservice client, imported at module level by
# src/api/main.py) -- same "worked in dev, missing here" class of gap as pyarrow above.
requests==2.34.2
# Streaming JSON parse of getPingsForDay -- see the note in requirements-anomaly.txt.
ijson==3.5.1

# Modeling (delay, GPS fallback, anomaly detection — chatbot excluded from this deployment)
torch==2.12.1
scikit-learn==1.9.0
joblib==1.5.3
prophet==1.3.0
filterpy==1.4.5
