# WiniCari AI layer — DEV/EXTRA dependencies, on top of requirements.txt.
# Needed for: the Streamlit dashboard, Jupyter notebooks, and the RAG chatbot module
# (none of which are part of the production API deployment — see requirements.txt).
-r requirements.txt

# Dashboard
streamlit==1.58.0
streamlit-option-menu==0.4.0
plotly==6.8.0
requests==2.34.2

# RAG chatbot (ENABLE_CHATBOT=true environments only)
chromadb==1.5.9
sentence-transformers==5.6.0
groq==1.5.0

# Notebooks (EDA / training exploration)
jupyterlab==4.6.0
notebook==7.6.0
ipykernel==7.3.0
matplotlib==3.11.0
